# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Dependency directories
vendor/

# Vendored pure-JS libs the bundler inlines: track the manifest, not the tree
jsdeps/node_modules/

# Go workspace file
go.work
go.work.sum

# Environment files
.env
